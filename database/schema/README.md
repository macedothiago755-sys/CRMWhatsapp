# Schema snapshots

Optional generated/reference schema snapshots (e.g. `pg_dump --schema-only` output) for diffing against
`database/migrations/`. Not required for Phase 0; the migrations directory is the canonical, executable
schema — see `docs/data/data-model.md`.
