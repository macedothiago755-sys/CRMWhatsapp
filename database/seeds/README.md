# Seed data

Local/development seed data only — **never** production data. Seeds are added alongside each phase's
tables as they're built out (e.g. `ai.agent`/`ai.tool` seed rows land with Phase 3, `consent.consent_purpose`
seed rows with Phase 2). Empty in Phase 0 beyond this note.
